<div class="container-fluid justify-content-between">
  <nav class="navbar navbar-expand-lg" >
    {% include ( "navbar_brand.html.twig" ) %}
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle " 
          data-bs-toggle="dropdown" 
          href=''
          role="button" 
          aria-expanded="false">{{ year }}
        </a>
        <ul class="dropdown-menu edt-dropdown-menu">
          {% set other_year = '2024' %}
          {% if year == '2024' %}
          {% set other_year = year + 1 %}
          {% endif %}
          <li><a class="dropdown-item edt-dropdown-item" href='{{ path('donateurs_homepage') }}/{{ other_year }}'>{{ other_year }}</a></li>
        </ul>
      </li>
    </ul>
      <div class="container-fluid">
            <p class="text-dark edt-donateurs-titre">{{ navbar_title }} {{ year }} - TOTAL {{ dons_total|number_format(2) }} Euros</li>
      </div>
  </nav>
</div>
