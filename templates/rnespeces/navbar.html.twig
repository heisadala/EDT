<div class="container-fluid justify-content-between">
  <nav class="navbar navbar-expand-lg" >
    {% include ( "navbar_brand.html.twig" ) %}
    <ul class="navbar-nav">
      {# <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle " 
          data-bs-toggle="dropdown" 
          href=''
          role="button" 
          aria-expanded="false">{{ year }}
        </a>
        <ul class="dropdown-menu edt-dropdown-menu">
          {% set other_year = '2024' %}
          {% if year == '2024' %}
          {% set other_year = year + 1 %}
          {% endif %}
          <li><a class="dropdown-item edt-dropdown-item" href='{{ path('especes_homepage') }}/{{ other_year }}'>{{ other_year }}</a></li>
        </ul>
      </li> #}
      {# {% if year == '2024' %}
        {% include ( "li_chart.html.twig" ) with { 'year' : year } %}
      {% endif %} #}
      {# {% include ( "li_table.html.twig" ) with { 'year' : year, 'filter' : 'DASH' }  %}
      {% include ( "li_dashboard.html.twig" ) with { 'year' : year, 'filter' : 'DASH' }  %}
      {% include ( "li_bilan.html.twig" ) with { 'year' : year, 'filter' : 'DASH' }  %}
      {% include ( "li_banque.html.twig" ) %} #}
    </ul>
		{% include ( "user_navbar.html.twig" ) %}
  </nav>
</div>
